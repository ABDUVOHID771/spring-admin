import { LoanRequestHistory } from './loan-request-history';

describe('LoanRequestHistory', () => {
  it('should create an instance', () => {
    expect(new LoanRequestHistory()).toBeTruthy();
  });
});
